
akka {
    //loglevel = DEBUG
	debug {
       receive = on

       autoreceive = on

       lifecycle = on

       fsm = on

       event-stream = on
     }

     actor {
     	debug {
	       receive = on

	       autoreceive = on

	       lifecycle = on

	       fsm = on

	       event-stream = on
     	}
     }
}
